<div class="card">
    <div class="card-body">
        <p class="fs-4"> <span class="mx-2 {{ $icon ?? 'fas fa-users' }}"></span>{{ $title ?? ''}}</p>
        <p class="mx-2 fw-bold fs-2">{{ $data ?? 0 }}</p>
    </div>
</div>
